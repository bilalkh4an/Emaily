/**
 * EmailReplyParser
 */
declare class EmailReplyParser {
    /**
     * Parse an email
     */
    read(text: string): import("./email.js").default;
    /**
     * Parse a reply
     */
    parseReply(text: string): string;
    /**
     * Parse a replied email
     */
    parseReplied(text: string): string;
}
export default EmailReplyParser;
